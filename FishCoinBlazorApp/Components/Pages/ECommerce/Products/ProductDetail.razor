@page "/product-details/{Id:int}"
@rendermode InteractiveServer
@using FishCoinBlazorApp.Services
@using FishCoinBlazorApp.Services.Models
@inject ProductService ProductService
@inject CartService CartService
@inject IJSRuntime JsRuntime;


<Topbar Title="პროდუქტი დეტალურად" />

@if (productDetail == null && !isNotFound)
{
    <div class="d-flex justify-content-center align-items-center" style="height: 50vh;">
        <div class="spinner-border text-primary" role="status">
            <span class="visually-hidden">იტვირთება...</span>
        </div>
    </div>
}

else if (isNotFound)
{
    @* თუ პროდუქტი ვერ მოიძებნა *@
    <div class="text-center p-5">
        <iconify-icon icon="solar:danger-bold-duotone" class="text-danger fs-64"></iconify-icon>
        <h3 class="mt-3">პროდუქტი ვერ მოიძებნა</h3>
        <p class="text-muted">სამწუხაროდ, მითითებული პროდუქტი არ არსებობს.</p>
        <a href="/products" class="btn btn-primary mt-2">დაბრუნება ჩამონათვალში</a>
    </div>
}

else
{
    <div class="row">
        <div class="col-lg-4">
            <div class="card h-100">
                <div class="card-body">
                    <div id="carouselExampleFade" class="carousel slide carousel-fade" data-bs-ride="carousel">
                        <div class="carousel-inner" role="listbox">
                            <div class="carousel-item active">
                                <img src="/images/product/p-1.png" alt="" class="img-fluid rounded shadow-sm">
                            </div>
                        </div>
                    </div>
                </div>
                <div class="card-footer border-top bg-light-subtle">
                    <div class="row g-2">
                        <div class="col-lg-6">
                            <button class="btn btn-primary ... gap-2 w-100 py-2 "
                                    disabled="@(productDetail.StockQuantity < 1)"
                                    @onclick="AddToCart">
                                <i class="bx bx-cart fs-20"></i>
                                @(productDetail.StockQuantity >= 1 ? "დამატება" : "ამოიწურა")
                            </button>
                        </div>
                        <div class="col-lg-6">
                            <button class="btn @(productDetail.StockQuantity >= 1 ? "btn-dark" : "btn-secondary") d-flex align-items-center justify-content-center gap-2 w-100 py-2"
                                    disabled="@(productDetail.StockQuantity < 1)">
                                <i class="bx bx-shopping-bag fs-20"></i>
                                @(productDetail.StockQuantity >= 1 ? "ყიდვა" : "მიუწვდომელია")
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-lg-8">
            <div class="card h-100">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-start">
                        <div>
                            <span class="badge bg-success-subtle text-success border border-success-subtle fs-12 px-2 py-1 uppercase">
                                @(productDetail.IsNew ? "ახალი მიღებული" : "")
                            </span>
                            <h3 class="mt-2 text-dark fw-semibold">@productDetail.Name</h3>
                        </div>
                        @* ქულების სისტემა ფასთან ახლოს *@
                        <div class="text-end">
                            <div class="bg-warning-subtle border border-warning-subtle rounded-3 p-2 d-inline-block text-center">
                                <span class="d-block fs-12 text-warning-emphasis fw-bold uppercase">ბონუსი</span>
                                <span class="fs-18 fw-bold text-dark">+@productDetail.PointsReward ქულა</span>
                                <i class='bx bxs-coin-stack text-warning ms-1'></i>
                            </div>
                        </div>
                    </div>

                    <div class="d-flex align-items-center gap-3 my-3">
                        @if (productDetail.DiscountPrecentage.HasValue)
                        {
                            <h2 class="fw-bold text-primary mb-0">@productDetail.DiscountPrice ₾</h2>
                            <h4 class="text-muted text-decoration-line-through mb-0 fs-18">@productDetail.Price ₾</h4>
                            <span class="badge bg-danger-subtle text-danger rounded-pill fs-12">-@productDetail.DiscountPrecentage% ფასდაკლება</span>

                        }
                        else
                        {
                            <h2 class="fw-bold text-primary mb-0">@productDetail.Price ₾</h2>
                        }
                    </div>

                    <div class="alert alert-info border-0 bg-info-subtle d-flex align-items-center gap-2 mt-3" role="alert">
                        <i class='bx bx-info-circle fs-20'></i>
                        <span class="fs-14">ამ პროდუქტის შეძენით თქვენს ბალანსზე დაგერიცხებათ <strong>@productDetail.PointsReward ქულა</strong>, რომელსაც გამოიყენებთ შემდეგ შენაძენზე.</span>
                    </div>

                    <div class="quantity mt-4">
                        <h5 class="text-dark fw-medium">რაოდენობა :</h5>
                        <div class="input-step border p-1 mt-2 rounded d-inline-flex">
                            <button type="button" class="minus btn btn-sm btn-light border-0">-</button>
                            <input type="number"
                                   class="text-center border-0 bg-transparent"
                                   value="@(productDetail.StockQuantity == 0 ? 0 : 1)"
                                   min="@(productDetail.StockQuantity == 0 ? 0 : 1)"
                                   max="@productDetail.StockQuantity"
                                   style="width: 50px;"
                                   disabled="disabled" />
                            <button type="button" class="plus btn btn-sm btn-light border-0">+</button>
                        </div>
                    </div>

                    <hr class="my-4">

                    <h5 class="text-dark fw-semibold">აღწერა :</h5>
                    <p class="text-muted lh-lg">@productDetail.Description</p>

                    <div class="row mt-3">
                        <div class="col-md-4">
                            <div class="d-flex align-items-center gap-2">
                                @* ფონი და იქონი იცვლება მარაგის მიხედვით *@
                                <div class="@(productDetail.StockQuantity >= 1 ? "bg-success-subtle" : "bg-danger-subtle") rounded-circle p-1 d-flex align-items-center justify-content-center">
                                    <i class='bx @(productDetail.StockQuantity >= 1 ? "bx-check text-success" : "bx-x text-danger") fs-18'></i>
                                </div>

                                @* ტექსტის ფერიც მიყვება სტატუსს *@
                                <span class="@(productDetail.StockQuantity >= 1 ? "text-success" : "text-danger") fw-medium fs-14">
                                    @(productDetail.StockQuantity >= 1 ? "მარაგშია" : "არ არის მარაგში")
                                </span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="row mt-4">
        <div class="col-lg-12">
            <div class="d-flex align-items-center justify-content-between mb-3">
                <h4 class="card-title mb-0">მსგავსი პროდუქტები</h4>
                <a href="/products" class="btn btn-sm btn-outline-primary">ყველას ნახვა</a>
            </div>
        </div>
    </div>

    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-4 g-3 mb-5">
        @if (relatedProducts != null)
        {
            @foreach (var item in relatedProducts)
            {
                <div class="col">
                    <div class="card h-100 shadow-sm border-0 position-relative overflow-hidden">

                        @if (item.DiscountPrecentage.HasValue && item.DiscountPrecentage > 0)
                        {
                            <div class="position-absolute top-0 start-0 m-2" style="z-index: 1;">
                                <span class="badge bg-danger rounded-pill shadow-sm">
                                    -@item.DiscountPrecentage%
                                </span>
                            </div>
                        }

                        <div class="p-2">
                            <a href="@($"/product-details/{item.Id}")">
                                <img src="/images/product/p-1.png" class="img-fluid rounded" alt="@item.Name">
                            </a>
                        </div>

                        <div class="card-body pt-3">
                            <a href="@($"/product-details/{item.Id}")" class="text-dark fw-bold fs-14 d-block text-truncate mb-2">
                                @item.Name
                            </a>

                            <div class="d-flex align-items-center justify-content-between">
                                <div>
                                    @if (item.DiscountPrecentage.HasValue && item.DiscountPrecentage > 0)
                                    {
                                        @* თუ არის ფასდაკლება: ახალი ფასი და გადახაზული ძველი *@
                                        <h5 class="text-primary fw-bold mb-0">@item.DiscountPrice ₾</h5>
                                        <small class="text-muted text-decoration-line-through">@item.Price ₾</small>
                                    }
                                    else
                                    {
                                        @* თუ არ არის ფასდაკლება *@
                                        <h5 class="text-primary fw-bold mb-0">@item.Price ₾</h5>
                                    }
                                </div>

                                <button class="btn btn-sm btn-soft-primary p-1 lh-1" @onclick="() => HandleRelatedAddToCart(item)">
                                    <i class="bx bx-plus fs-18"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            }
        }
    </div>

    @* გაუმჯობესებული Review სექცია *@
    <div class="row mt-4">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-header bg-transparent border-bottom d-flex justify-content-between align-items-center">
                    <h4 class="card-title mb-0">მომხმარებელთა შეფასებები</h4>
                </div>
                <div class="card-body">
                    <div class="row">
                        @* მარცხენა მხარე - რეიტინგების შეჯამება *@
                        <div class="col-md-4 border-end">
                            <div class="text-center py-3">
                                <h1 class="display-4 fw-bold text-dark">4.5</h1>
                                <div class="text-warning fs-20 mb-2">
                                    <i class="bx bxs-star"></i>
                                    <i class="bx bxs-star"></i>
                                    <i class="bx bxs-star"></i>
                                    <i class="bx bxs-star"></i>
                                    <i class="bx bxs-star-half"></i>
                                </div>
                                <p class="text-muted">საერთო რეიტინგი (128 შეფასება)</p>
                            </div>
                        </div>

                        @* მარჯვენა მხარე - კონკრეტული რევიუები სქროლით *@
                        <div class="col-md-8">
                            @* დავამატეთ სტილი: max-height და overflow-y *@
                            <div class="px-3 review-scroll-container" style="max-height: 450px; overflow-y: auto; scrollbar-width: thin;">

                                <div class="d-flex gap-3 mb-4 pe-2">
                                    <img src="/images/users/avatar-6.jpg" alt="" class="avatar-md rounded-circle border p-1">
                                    <div class="flex-grow-1">
                                        <div class="d-flex justify-content-between">
                                            <h5 class="mb-1">Henny K. Mark <span class="badge bg-light text-muted fw-normal ms-2 fs-11">Verified Purchase</span></h5>
                                            <small class="text-muted">16 Nov 2023</small>
                                        </div>
                                        <div class="text-warning mb-2">
                                            <i class="bx bxs-star fs-14"></i><i class="bx bxs-star fs-14"></i><i class="bx bxs-star fs-14"></i><i class="bx bxs-star fs-14"></i><i class="bx bxs-star fs-14 text-muted"></i>
                                        </div>
                                        <h6 class="text-dark fw-semibold">Excellent Quality</h6>
                                        <p class="text-muted fs-14">Medium thickness. Did not shrink after wash. Good elasticity. XL size Perfectly fit for 5.10 height and heavy body. Highly recommended!</p>

                                    </div>
                                </div>

                                <hr class="text-light">

                                <div class="d-flex gap-3 mb-4 pe-2">
                                    <img src="/images/users/avatar-4.jpg" alt="" class="avatar-md rounded-circle border p-1">
                                    <div class="flex-grow-1">
                                        <div class="d-flex justify-content-between">
                                            <h5 class="mb-1">Jorge Herry</h5>
                                            <small class="text-muted">21 Dec 2023</small>
                                        </div>
                                        <div class="text-warning mb-2">
                                            <i class="bx bxs-star fs-14"></i><i class="bx bxs-star fs-14"></i><i class="bx bxs-star fs-14"></i><i class="bx bxs-star fs-14"></i><i class="bx bxs-star fs-14"></i>
                                        </div>
                                        <h6 class="text-dark fw-semibold">Pure Cotton!</h6>
                                        <p class="text-muted fs-14 mb-0">I liked the tshirt, it's pure cotton & skin friendly, but the size is smaller to compare standard size.</p>
                                    </div>
                                </div>

                                <div class="d-flex gap-3 mb-4 pe-2">
                                    <img src="/images/users/avatar-4.jpg" alt="" class="avatar-md rounded-circle border p-1">
                                    <div class="flex-grow-1">
                                        <div class="d-flex justify-content-between">
                                            <h5 class="mb-1">Jorge Herry</h5>
                                            <small class="text-muted">21 Dec 2023</small>
                                        </div>
                                        <div class="text-warning mb-2">
                                            <i class="bx bxs-star fs-14"></i><i class="bx bxs-star fs-14"></i><i class="bx bxs-star fs-14"></i><i class="bx bxs-star fs-14"></i><i class="bx bxs-star fs-14"></i>
                                        </div>
                                        <h6 class="text-dark fw-semibold">Pure Cotton!</h6>
                                        <p class="text-muted fs-14 mb-0">I liked the tshirt, it's pure cotton & skin friendly, but the size is smaller to compare standard size.</p>
                                    </div>
                                </div>

                                <div class="d-flex gap-3 mb-4 pe-2">
                                    <img src="/images/users/avatar-4.jpg" alt="" class="avatar-md rounded-circle border p-1">
                                    <div class="flex-grow-1">
                                        <div class="d-flex justify-content-between">
                                            <h5 class="mb-1">Jorge Herry</h5>
                                            <small class="text-muted">21 Dec 2023</small>
                                        </div>
                                        <div class="text-warning mb-2">
                                            <i class="bx bxs-star fs-14"></i><i class="bx bxs-star fs-14"></i><i class="bx bxs-star fs-14"></i><i class="bx bxs-star fs-14"></i><i class="bx bxs-star fs-14"></i>
                                        </div>
                                        <h6 class="text-dark fw-semibold">Pure Cotton!</h6>
                                        <p class="text-muted fs-14 mb-0">I liked the tshirt, it's pure cotton & skin friendly, but the size is smaller to compare standard size.</p>
                                    </div>
                                </div>

                                <div class="d-flex gap-3 mb-4 pe-2">
                                    <img src="/images/users/avatar-4.jpg" alt="" class="avatar-md rounded-circle border p-1">
                                    <div class="flex-grow-1">
                                        <div class="d-flex justify-content-between">
                                            <h5 class="mb-1">Jorge Herry</h5>
                                            <small class="text-muted">21 Dec 2023</small>
                                        </div>
                                        <div class="text-warning mb-2">
                                            <i class="bx bxs-star fs-14"></i><i class="bx bxs-star fs-14"></i><i class="bx bxs-star fs-14"></i><i class="bx bxs-star fs-14"></i><i class="bx bxs-star fs-14"></i>
                                        </div>
                                        <h6 class="text-dark fw-semibold">Pure Cotton!</h6>
                                        <p class="text-muted fs-14 mb-0">I liked the tshirt, it's pure cotton & skin friendly, but the size is smaller to compare standard size.</p>
                                    </div>
                                </div>

                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
}

@if (showToast)
{
    <div class="position-fixed bottom-0 end-0 p-3" style="z-index: 1050">
        <div class="toast show align-items-center text-white bg-primary border-0 shadow-lg ripple-animation" role="alert">
            <div class="d-flex">
                <div class="toast-body d-flex align-items-center gap-2">
                    <i class="bx bx-check-circle fs-20"></i>
                    <div>
                        <strong>დაემატა!</strong><br />
                        პროდუქტი დაემატა კალათაში.
                    </div>
                </div>
                <button type="button" class="btn-close btn-close-white me-2 m-auto" @onclick="() => showToast = false"></button>
            </div>
        </div>
    </div>
}

@code {
    private IJSObjectReference? _module;

    private ProductDetailModel? productDetail;
    private bool showToast = false;
    private bool isNotFound = false;
    private List<ProductDetailModel>? relatedProducts;

    [Parameter]
    public int Id { get; set; }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            // ეს ხაზი აიტანს სქროლს ზემოთ მხოლოდ ამ გვერდზე შემოსვლისას
            await JsRuntime.InvokeVoidAsync("window.scrollTo", 0, 0);
            _module = await JsRuntime.InvokeAsync<IJSObjectReference>("import", "./js/pages/ecommerce-product-details.js");
            await JsRuntime.InvokeVoidAsync("loadProductDetails");
            await JsRuntime.InvokeVoidAsync("loadThemeConfig");
            await JsRuntime.InvokeVoidAsync("loadApps");
        }
    }

    protected override async Task OnParametersSetAsync()
    {
        await OnInitializedAsync();
    }

    protected override async Task OnInitializedAsync()
    {
        productDetail = await ProductService.GetProductById(Id);

        if (productDetail == null)
        {
            isNotFound = true;
        }
        else
        {
            relatedProducts = await ProductService.GetProductByCategory(Id);
        }
    }

    private async Task HandleRelatedAddToCart(ProductDetailModel item)
    {
        await CartService.AddToCart(item, 1);

        showToast = true;
        StateHasChanged();

        await Task.Delay(3000);
        showToast = false;
        StateHasChanged();
    }

    private async Task AddToCart()
    {
        if (productDetail != null)
        {
            await CartService.AddToCart(productDetail, 1); // ვაწვდით ობიექტს
            showToast = true;
            StateHasChanged();

            await Task.Delay(3000);
            showToast = false;
            StateHasChanged();
        }
    }
}